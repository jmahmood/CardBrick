"""CardBrick-Py: an offline Anki-format flashcard reviewer for Linux handhelds.

Uses .apkg files as a content format only. After import, the application
runs entirely from its own SQLite database, scheduled by py-fsrs and
rendered with pygame. No Anki runtime, no Rust, no network.
"""

__version__ = "0.2.0"
